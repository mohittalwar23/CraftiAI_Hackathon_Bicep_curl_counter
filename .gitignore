# ESP-IDF build artifacts (never ship these in a source package)
build/
managed_components/
sdkconfig.old
*.bin
*.elf
*.map

# Firmgen
.firmgen_sources

# Real credentials: never commit this. Copy from main/app_config.example.h
main/app_config.h

# FirmGen's internal project-intelligence cache: local tool state, regenerated
# automatically, and it can embed live config values (including secrets) verbatim.
.craftif/
